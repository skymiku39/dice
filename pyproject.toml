[project]
name = "skymiku-dice"
version = "2.0.0"
description = "多方向 3D 骰子（GitHub Pages + OBS + 本機控制橋）"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "aiohttp>=3.9",
]

[tool.uv]
package = false
